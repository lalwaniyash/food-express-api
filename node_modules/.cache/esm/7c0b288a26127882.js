let CustomErrorHandler,JwtService;_261‍.x([["default",()=>_261‍.o]]);_261‍.w("../services/CustomErrorHander",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_261‍.w("../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);


const auth = async (req, res, next) => {
    let authHeader = req.headers.authorization;
    if (!authHeader) {
        return next(CustomErrorHandler.unAuthorized());
    }

    const token = authHeader.split(' ')[1];

    try {
        const { _id, role } = await JwtService.verify(token);
        const user = {
            _id,
            role
        }
        req.user = user;
        next();

    } catch(err) {
        return next(CustomErrorHandler.unAuthorized());
    }

}

_261‍.d(auth);