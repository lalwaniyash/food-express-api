let DEBUG_MODE,ValidationError,CustomErrorHandler;_659‍.x([["default",()=>_659‍.o]]);_659‍.w("../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_659‍.w("joi",[["ValidationError",["ValidationError"],function(v){ValidationError=v}]]);_659‍.w("../services/CustomErrorHander",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);
 


const errorHandler = (err, req, res, next) => {
    let statusCode = 500;
    let data = {
        message: 'Internal server error',
        ...(DEBUG_MODE === 'true' && { originalError: err.message })
    }

    if(err instanceof ValidationError) {
        statusCode = 422;
        data = {
            message: err.message
        }
    }
    
    if(err instanceof CustomErrorHandler) {
        statusCode = err.status;
        data = {
            message: err.message
        }
    }

    return res.status(statusCode).json(data);
}

_659‍.d(errorHandler);